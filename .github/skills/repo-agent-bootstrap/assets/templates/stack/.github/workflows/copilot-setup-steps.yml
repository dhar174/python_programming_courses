name: copilot-setup-steps

on:
  workflow_dispatch:

jobs:
  verify-agent-stack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Replace with repo-specific validation commands."
